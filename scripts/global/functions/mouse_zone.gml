#define mouse_zone
//mousezone(x1,y1,x2,y2)
//tests if the mouse is within zone

if(mouse_x > argument0 && mouse_x < argument2 && mouse_y > argument1 && mouse_y < argument3)
    return true
else 
    return false
